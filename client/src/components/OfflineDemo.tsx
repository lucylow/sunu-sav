// client/src/components/OfflineDemo.tsx
// NOTE: This component contains React Native imports and needs to be converted to web components
// For now, providing a placeholder to fix TypeScript errors

import React from 'react';

interface OfflineDemoProps {
  // Add props as needed
}

const OfflineDemo: React.FC<OfflineDemoProps> = () => {
  return (
    <div className="p-4 border rounded-lg">
      <h3 className="text-lg font-semibold mb-2">Offline Demo</h3>
      <p className="text-gray-600">
        Offline demo functionality is being converted from React Native to web components.
        This is a placeholder component.
      </p>
    </div>
  );
};

export default OfflineDemo;